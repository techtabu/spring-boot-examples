server:
  port: 9874

spring:
  data:
    mongodb:
      database: store_orders
      port: 27017

data:
  initialize: false

logging:
  level:
    root: info
    org.springframework.data.mongodb: DEBUG
